name: agente-invalido-secret
purpose: "Fixture inválido con un secreto simulado en texto plano"
triggers:
  - issue_created
inputs:
  - issue_id
outputs:
  - result
permissions:
  read:
    - repo:readonly
actions_allowed:
  - create_branch
human_approval_required: []
observability:
  logs: true
rate_limits:
  tasks_per_minute: 1
notes: "api_token: ghp_123456789012345678901234567890"
